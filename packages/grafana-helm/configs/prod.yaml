namespace: "grafana"
chart_version: "7.0.0"
admin_password: "admin123"  # In production, use a secure password
persistence:
  enabled: true
  size: "50Gi"
service:
  type: "NodePort"
  port: 80
  nodePort: 30000
values:
  grafana:
    adminPassword: "admin123"
    persistence:
      enabled: true
      size: "50Gi"
    service:
      type: "NodePort"
      port: 80
      nodePort: 30000
